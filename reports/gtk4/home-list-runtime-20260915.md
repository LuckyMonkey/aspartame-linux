# GTK4 Home List runtime verification — 2026-09-15

The semantic `ShowList()` action returned `1` in the live guest and rendered
the GTK4 Home list at 1920×1080. The capture shows installed activity icons,
names, descriptions, versions, and explicit runtime status (including the
Classic Space distinction for an unavailable legacy Calculate bundle).

Evidence: `reports/screenshots/sugar-20260915-080238-v0.0.31.png` and OCR
sidecar.
