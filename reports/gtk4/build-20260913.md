# GTK4 preview build evidence — 2026-09-13

The guest build was rerun after synchronizing the development share with the
repository at commit `5d23c4e`.

```text
./scripts/ssh-asp 'timeout 300 /mnt/aspartame-dev/scripts/sugar-gtk4-build.sh'
```

The build now treats already-present semantic results as idempotent for:

- `0015-toolkit-cell-renderer-props-compat.patch` (CellRendererIcon contract)
- `0055-empty-space-view-state.patch` (empty Group state)
- `0068-toolkit-menuitem-set-image.patch` (GTK4 MenuItem icon API)
- `0074-main-home-key-capture.patch` (Home surface shortcut controller)

It completed with:

```text
GTK4 toolkit, Casilda, sugar-ext, Jarabe, and datastore preview build: PASS
Wayland socket will be owned by embedded Casilda at runtime
```

Runtime smoke check after restart:

```text
runtime-check=ok target=gtk4 pid=98527 desktop=1 window=0x1000005 stable_pid=86065 gtk4_pid=98527
```

The QEMU framebuffer remained 1920×1080. The F-key injection helper did not
produce GTK key events in this QEMU window while ordinary text input did; this
is retained as a harness/input-capture issue, not reported as successful
physical-key validation.
