#!/usr/bin/env bash
set -euo pipefail

# activityinstance is a console-script module, not a runnable -m module.
# Call the same entrypoint as the toolkit's installed console script.
exec "${PYTHON_BIN:-python3}" -c \
    'from sugar4.activity.activityinstance import main; main()' "$@"
