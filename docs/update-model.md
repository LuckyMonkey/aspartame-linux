# Update model

Development uses normal Arch repositories and pacman. Future Aspartame work
should evaluate a curated repository, delayed/snapshotted Arch repositories,
automatic Btrfs snapshots, rollback, and Flatpak for applications. No custom
package manager is introduced before the base system is reliable.

