From: Aspartame GTK4 migration
Subject: [PATCH] gtk4: finalize Frame accessibility placement

Repair the remaining drift from 0119 by placing the accessibility calls
between `_position` initialization and the orientation branch.

--- a/src/jarabe/frame/framewindow.py
+++ b/src/jarabe/frame/framewindow.py
@@ -52,10 +52,10 @@
     def __init__(self, position):
         super().__init__()
         self._position = position
+        self.update_property([Gtk.AccessibleProperty.LABEL], [_('Frame')])
+        self.set_accessible_role(Gtk.AccessibleRole.GROUP)
 
         if self.is_vertical():
             self._box = Gtk.Box(orientation=Gtk.Orientation.VERTICAL)
         else:
-        self.update_property([Gtk.AccessibleProperty.LABEL], [_('Frame')])
-        self.set_accessible_role(Gtk.AccessibleRole.GROUP)
             self._box = Gtk.Box(orientation=Gtk.Orientation.HORIZONTAL)
